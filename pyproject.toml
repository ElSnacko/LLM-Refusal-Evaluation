[project]
name = "oss-refusal-evaluation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.5.0",
    "matplotlib>=3.9.4",
    "pyyaml>=6.0.3",
    "vllm>=0.11.0",
]

[project.optional-dependencies]
gguf = ["llama-cpp-python>=0.3.0"]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
