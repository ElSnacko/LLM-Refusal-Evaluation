[project]
name = "oss-refusal-evaluation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.6.0",
    "matplotlib>=3.10.8",
    "pyyaml>=6.0.3",
    "vllm>=0.16.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
