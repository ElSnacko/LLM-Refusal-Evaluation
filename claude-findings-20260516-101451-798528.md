# Claude Code Findings

**Iteration:** 1

## stdout

```
All tests pass. Here's a summary of the bugs I fixed:

## Summary of Bugs Fixed

### 1. BUG-061: label=None crash in compute_refusal_score.py:218
**Issue**: When a judge score contains `{"label": None}`, the `.get("label", 0.0)` returns `None` (not `0.0`), causing `float(None)` to raise `TypeError`.

**Fix**: Changed to explicitly check for None before converting:
```python
label_raw = example_judges[k].get("label")
if label_raw is not None:
    label_val = float(label_raw)
```

### 2. BUG-036: Case-insensitive column exclusion in compute_refusal_score.py
**Issue**: `_NON_CATEGORY_BOOLS` comparison was case-sensitive, so columns like `"Is_Safe"` or `"IS_SAFE"` were not being excluded from category detection.

**Fix**: Renamed to `_NON_CATEGORY_BOOLS_LOWER` and use `.lower()` comparison:
```python
and k.lower() not in _NON_CATEGORY_BOOLS_LOWER
```

### 3. Typo: max_new_tokens default in llm_judge.py:85
**Issue**: Default value was `8196` instead of the standard `8192`.

**Fix**: Changed to `8192`.

```json
{
  "hypothesis": "Fixed label=None crash handling, case-insensitive column exclusion, and max_new_tokens typo",
  "finding": "Fixed 3 real bugs: label=None causing TypeError, case-sensitive column exclusion missing variants, and typo in default token count",
  "metrics": {"bugs_fixed": 3},
  "artifacts": ["src/compute_refusal_score.py", "src/llm_judge.py"]
}
```
```

## stderr

```
Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.

```
